$('.delete_printer_consommable').bind('ajax:success', function() {  
  $(this).closest('tr').fadeOut();
});  

//Delete consommable /consommables
$('.delete_consommable').bind('ajax:success', function() {  
  window.location.reload();
});  
