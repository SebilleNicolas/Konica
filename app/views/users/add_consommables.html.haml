%H1 
  Les Consommables ajouter par 
  %b= @user.last_name 
  %b= @user.first_name

%table.table-bordered{:style => "width: 940px; text-align: center; "}
  %thead
    %th{:style => "text-align: center;"} Date
    %th{:style => "text-align: center;"} ID consommable
    %th{:style => "text-align: center;"} Code consommable
    %th{:style => "text-align: center;"} Designation consommable
    %th{:style => "text-align: center;"} Durée de vie consommable
  %tbody
    - @user_add_consommables.each do |i| 
      / - puts i.incident_id
      - @@consommable = Consommable.find(i.consommable_id)
      %tr
        %td
          = i.date_add
          = i.hour_add+":"+i.minute_add
          
        
        %td= i.consommable_id
        %td= @@consommable.code_consommables
        %td= @@consommable.designation_consommables
        %td= @@consommable.duree_vie_consommables + " jours"
        / %td= @@consommable.intitule_consommables