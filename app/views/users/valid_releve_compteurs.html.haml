%H1 
  Les relevé compteur validés par 
  %b= @user.last_name 
  %b= @user.first_name

%table.table-bordered{:style => "width: 940px; text-align: center; "}
  %thead
    %th{:style => "text-align: center;"} Date
    %th{:style => "text-align: center;"} ID relevé compteur
  %tbody
    - @admin_valid_releve_compteur.each do |i| 
      / - puts i.incident_id
      - @@releve_compteur = ReleveCompteur.find(i.releve_compteur_id)
      %tr
        %td
          =  i.date_valid 
          =  i.hour_valid+":"+i.minute_valid
        %td= link_to i.releve_compteur_id , releve_compteur_path(@@releve_compteur)
